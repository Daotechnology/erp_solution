

export const checkPermission = async (roles: string[], resourceSlug: string): boolean => {
    //fetch the permission ids from the role and permission relationship
    // const permissionAndRoleRelationship = await ;
    return true;
}
